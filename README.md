linkedin profile html parser

quick tool I threw together to grab text out of saved linkedin profile html pages without dealing with selenium or playwright.

setup:

pip install -r requirements.txt

usage:

python parser.py path/to/profile.html --out profile.json

<!-- checked: 2026-09-14 -->
